version: "2"

linters:
  default: standard
  disable:
    # errcheck produces false positives on pre-existing upstream code that
    # we do not own (cert.go, gontlm-proxy.go defers, helpers_darwin.go).
    # Re-enable once upstream is patched.
    - errcheck
